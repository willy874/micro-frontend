{
  "name": "micro-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev -w @micro-app/builder && npm run dev -w @micro-app/framework && npm run dev -w @micro-app/sub-app && npm i"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=16.14.0",
    "npm": ">=7.24.2"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "ramda": "^0.28.0"
  }
}
